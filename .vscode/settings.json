{
    "files.trimTrailingWhitespace": true,
    "command-runner.commands": {
        "Package": "cd ${workspaceFolder} && wsl vsce package --out ./Build/",
        "Publish": "cd ${workspaceFolder} && wsl vsce publish",
    },
    "[yaml]": {
        "editor.renderWhitespace": "boundary",
    }
}
